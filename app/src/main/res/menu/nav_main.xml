<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Add Reminder", should appear as action button if possible -->
    <item
        android:id="@+id/nav_action_add"
        android:icon="@drawable/ic_action_add"
        android:title="@string/nav_action_add"
        app:showAsAction="ifRoom"/>

    <!-- Logout, should always be in the overflow -->
    <item android:id="@+id/nav_action_logout"
        android:title="@string/nav_action_logout"
        app:showAsAction="never"/>

</menu>